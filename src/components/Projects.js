import React from 'react';
import styled from 'styled-components';

const Section = styled.section`
  padding: 20px;
`;

const Projects = () => (
  <Section>
    <h2>Section 2 Placeholder</h2>
    <p>Placeholder text.</p>
  </Section>
);

export default Projects;
