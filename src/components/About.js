import React from 'react';
import styled from 'styled-components';

const Section = styled.section`
  padding: 20px;
`;

const About = () => (
  <Section>
    <h2>About This Page</h2>
    <p>Testing a React App on Github Pages</p>
  </Section>
);

export default About;
